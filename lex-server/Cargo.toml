[package]
name = "lex-server"
version.workspace = true
edition.workspace = true

[dependencies]
anyhow.workspace = true
fastrace = { workspace = true, features = ["enable"] }
lex-http.workspace = true
tokio = { workspace = true, features = ["macros", "rt-multi-thread"] }
